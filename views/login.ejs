<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Placement System</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/bootstrap/5.3.0/css/bootstrap.min.css">
    <style>
        body {
            font-family: Arial, sans-serif;
            background: url('https://dubaimonsters.com/wp-content/uploads/2018/04/High-Resolution-Background-Images.jpg') 
            no-repeat center center fixed;
            background-size: cover;
            color: white;
        }
        /* Navbar Styling */
        .navbar {
            background-color: rgba(0, 0, 0, 0.7);
            display: flex;
            justify-content: center;
            padding: 12px 0;
        }
        .navbar a {
            color: white;
            text-decoration: none;
            font-size: 16px;
            padding: 10px 18px;
            margin: 0 10px;
            border-radius: 5px;
        }
        .navbar a:hover {
            background-color: rgba(255, 255, 255, 0.3);
        }
        /* Title Styling */
        .title {
            text-align: center;
            color: rgb(252, 250, 250);
            font-size: 70px; /* Increased font size */
            margin-top: 120px;
            margin-bottom: 20px;
            text-shadow: 3px 3px 6px rgba(0, 0, 0, 0.6);
        }
        /* Login Box */
        .login-box {
            background-color: rgba(255, 255, 255, 0.9);
            padding: 20px;
            border-radius: 5px;
            box-shadow: 0px 0px 10px rgba(0, 0, 0, 0.2);
            max-width: 400px;
            margin: auto;
            color: rgb(13, 13, 13);
        }
    </style>
</head>
<body>

    <!-- Navigation Bar -->
    <nav class="navbar">
        <a href="#">HOME PAGE</a>
        <a href="#">LOGIN</a>
        <a href="#">STUDENT REGISTER</a>
        <a href="#">COMPANY REGISTER</a>
    </nav>

    <!-- Title Section -->
    <div class="title">SMART PLACEMENT SYSTEM</div>

    <!-- Login Form -->
    <div class="login-box">
        <h4 class="text-center">Login</h4>
        <form>
            <div class="mb-3">
                <label class="form-label">Username</label>
                <input type="text" class="form-control" placeholder="Enter username">
            </div>
            <div class="mb-3">
                <label class="form-label">Password</label>
                <input type="password" class="form-control" placeholder="Enter password">
            </div>
            <div class="d-flex justify-content-between">
                <button type="submit" class="btn btn-primary">Submit</button>
                <button type="reset" class="btn btn-secondary">Reset</button>
            </div>
        </form>
    </div>

    <!-- Footer -->
    <div class="footer"></div>

</body>
</html>

